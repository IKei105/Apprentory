const pageDataElement = document.getElementById('page-data');

const loggedInUserId = pageDataElement?.dataset.loggedInUserId ?? null;
const followTargetUserId = pageDataElement?.dataset.followTargetUserId ?? null;
const discordId = pageDataElement?.dataset.discordId ?? null;
